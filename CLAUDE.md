# CLAUDE.md — 프로젝트 가드레일

## 이 프로젝트는
동물이 주인인 장소에서 재료를 훔쳐다, 이동식 기지(푸드트럭 프레임)에서 요리해 파는 1~4인 협동 추출 게임. 지금은 **웹 프로토타입 단계**다.

## 진실의 원천
- 설계: `docs/design-cooking-extraction-20260809.md` — 모든 판단은 이 문서 기준. 문서와 충돌하는 요청이 오면 작업 전에 먼저 지적할 것.
- 현재 태스크: `StartProject.md`

## 범위 규칙 (어기지 말 것)
- 신규 시스템 1개 원칙. 이번 프로토의 범위는 StartProject.md에 명시된 것이 전부다.
  - **v0.6 라운드는 개발자 결정으로 예외** (2026-08-11, "한 번에 v0.6"): 시야 차단 + 정전 타이머 + 닭 지능 4종 + 글러브·닭고기 + 2슬롯 + 수레 + 점프를 한 판에 넣었다. 검증 변수 증가 리스크는 개발자 수용. 상세: `docs/goal/chicken-heist-dark-surprise.md`. **v0.7부터는 원칙 복원.**
  - 같은 세션에서 "무기 없음"(2026-08-10 결정)도 개발자 요청으로 번복됨 — 권투 글러브 1종.
    - **"닭은 죽지 않는다(KO만)" 번복** (v0.17, 2026-08-17 개발자 "닭이 죽게 해줘. 모든 몬스터는 공략할수있어야돼"): 쓰러진 닭을 한 번 더 치면 영구 제거된다. **즉사 금지와 충돌하지 않는다** — 그 규칙은 플레이어의 실패를 죽음이 아니라 코미디로 처리하라는 것이고, 지금도 그렇다.
    - 진짜 위험은 "먼저 다 잡고 천천히 턴다"가 지배 전략이 되는 것이다. 실측 손익분기가 **14%**였다(B1 지킴이 4마리 사살 = 밤의 20%, 왕복 1회 손실). 그래서 대가를 **시간이 아니라 소리**로 붙였다: 죽는 소리는 `KILL_ALARM_R=700`으로 같은 층에 퍼지고, 들은 닭은 조사가 아니라 `KILL_RAGE_T=9`s 사냥에 나선다(실측 4/5마리 전환, 3초에 150~270u 접근). **연쇄 사살의 대가는 점점 뜨거워지는 층이다.**
    - `KILL_ALARM_R`은 **자가검사 10(반경 < 존 이격)의 대상이 아니다** — `killChicken` 안에서 `zoneOfArea` 동일성을 명시 검사하므로 벽·존을 무시하지 않는다. 검사 10에 넣지 말 것.
    - 고기는 여전히 **첫 KO에 마리당 1회**다(사살로 추가 고기 없음) — 상한 10 유지.
    - **v0.20에서 대가 (b)의 근거 정정·이름 이전**: "쿨 2.5초 = 두 방 사이 서 있기"의 실측 억제력은 이동시간에 흡수되어 4~5초뿐이었다. 연사는 0.70초(=2×PUSH_STAGGER)로 풀고, 사살 대가는 **확인사살 전용 재장전 `KILL_CD=KILL_RAGE_T`(9초)**로 옮겼다 — "다음 사살은 지난 사살이 만든 격노가 끝난 뒤에야". 실측 B1 스윕 실비용 26.5% vs 손익분기 31.1% = 지배 아닌 빠듯한 선택(1.17×). KILL_CD와 KILL_RAGE_T는 한 손잡이다 — 따로 움직이지 말 것.
- **"플레이어 HP는 설계상 없음" 파기** (v0.20, 2026-08-17 개발자 "내 hp가 없고, 한방에 죽는거 별로"): HP 3칸 신설 — 이번 라운드의 신규 시스템 1개. 완충타는 알을 안 뺏고(짧은 비틀 + 닭 flap 후퇴), **HP 0째 타격 = 기존 넉다운 그대로 + HP 재충전** — 신규 실패 상태·리스폰·강제이동 0개라 즉사 금지 무변경. 실측: 넉다운 -77%, 반출률 불변(HP는 경제가 아니라 체감을 바꾼다), 강탈 4.2회/밤으로 닭의 도둑 동사 생존. `stats.caught`는 넉다운만 센다(정산 "잡힘"의 의미 보존). 회복 채널을 따로 만들지 말 것 — 넉다운 재충전만으로 균형이 잡혀 있다.
  - **v0.15 라운드도 일괄 예외** (2026-08-15, 개발자 확정 "풀 랜덤 맵 일괄"): 요리(보온등 주방) + 시드 절차 생성 맵 + 미니맵 + 전면 감광을 한 판에. 검증 변수 급증 리스크 개발자 수용, 완화책은 `?baked=1`(구운 v0.14 맵)·`?bright=1`(구 밝기) dev A/B 플래그 — 친구 링크엔 절대 미포함. 상세: `docs/goal/chicken-heist-procgen-kitchen.md`. **v0.16부터 원칙 재복원.**
  - **v0.21 라운드도 일괄 예외** (2026-08-18, 개발자 확정 "4종 일괄"): 너구리 + 몰래 리젠 + 가격제 + 선글라스 구매 (+수레 야광 렌더). 4관점 패널+교차심문(9 에이전트)으로 설계 확정 후 구현 — 결정·기각 사유는 `docs/goal/chicken-heist-regen-appraisal.md`의 Design Decisions 절이 진실이고 **구현 중 재론 금지가 원칙**. 사후 실측 숙제 4건(조기 B1 스윕 비용·GRADE 재보정·조기 순찰 커버리지·정보 픽 비율)은 같은 문서 Existing Integration Contract 절. **v0.22부터 원칙 재복원.**
- 다음은 설계상 보류함·폐기 항목이므로 구현하지도, 제안하지도 말 것:
  요리·열 상태변화 / 상점·구매·스탯 / 주문(쿼터) 시스템 / 멀티플레이·넷코드 / 부패·허기 / 영업 러시 / 닭 외 동물 추가
  - **"닭 외 동물 추가" 금지 해제** (2026-08-14, 개발자 승인 — "시련이 너무 적어서 재미가 없어"에 대한 판단으로 금지 해제 선택): 새 동물을 위협으로 추가할 수 있다. 해제할 때 같이 확인된 비용: 설계 문서 핵심 결정 3("동물이라 왜-질문이 공짜다")이 약해지므로, 새 동물은 **양계장에 있을 이유가 설명 없이 읽히는 종**이어야 한다. 몬스터는 여전히 아니다.
    - **v0.16에서 회수됨** (2026-08-16, 개발자 "아예 없애달라니까"): 거위 삭제(`GOOSE_N=0`·`GOOSE_HOME=[]` — FSM은 휴면 보존). 마당은 다시 무위협 통로. ~~동물 추가 카드는 여전히 소진 상태~~
    - **v0.21에서 카드 재사용 — 너구리 2마리** (2026-08-18, 개발자 "닭말고도 몬스터 더 배치해줘" + 종 확정 질의에 "너구리 확정". 같은 질의에서 "양계장 아니어도 돼" — **핵심 결정 3의 '읽히는 종' 조건을 개발자 권한으로 완화**했으나, 채택된 너구리는 어차피 그 조건을 통과한다): 마당 전담 **재산 포식자** — 방치된(활성 플레이어 120u 밖) 바닥 알·수레 짐만 훔쳐 굴로 끌고 간다. 플레이어 무접촉·무피해, 한 대 맞으면 전부 떨구고 밤 퇴장(KO도 확인사살도 없음 — **사살 경제 KILL_CD와 접점 0**). 거위 선례 둘 다 준수: ① 닭 AI 복제 0(닭=든 알 강탈/실내, 너구리=방치 짐 절도/마당 — 생태계에 없던 역할), ② 구역 격리(`RACC_MAX_X` 하드라인, 축사 진입 불가). 초소 2점은 THREATS 루프(r=THREAT_ACT_MIN_R)로 로드타임 검증 — **초소는 위협 홈이지 yardChokes 행이 아니다** (행으로 넣으면 자기 자신과의 검사로 항상 실패). 끌기 100 < 운반 질주 152.6 부등식이 자가검사에 있다. 상세: `docs/goal/chicken-heist-regen-appraisal.md`.
    - **v0.14에서 소진됨** (2026-08-15): 마당 경비 거위 2마리. 상세: `docs/goal/chicken-heist-guard-goose.md`. 다음 동물을 또 추가할 때도 위 "설명 없이 읽히는 종" 조건은 그대로 적용된다. 거위가 세운 선례 두 가지를 지킬 것 — ① 새 동물은 **닭 AI를 복제하지 않는다** (역할이 달라야 추가할 이유가 있다: 닭=도둑, 거위=경비), ② **활동 구역을 격리한다** (거위는 마당 전담·축사 문턱 못 넘음 → 닭 시스템과 간섭 0, 검증 변수도 그 구역에 갇힌다).
  - **"상점·구매·스탯" 부분 해제** (v0.21, 2026-08-18 개발자 "선글라스는 푸드트럭에서 살 수 있어야"): 설계 문서 원문 코어 루프에 "납품 · 구매 (프라이팬 = 2계란)"가 원래 있으므로 요리처럼 **해제가 아니라 복귀**다. 설계 문서 공식 미해결 질문 "구매 화폐의 단위"는 개발자 확정으로 **판매 대금(수확 점수 차감)** 채택. 단 열린 건 딱 여기까지다 — **판매 품목은 선글라스 1종(30점·1인당·판당), 스탯·업그레이드·HUD 상점 메뉴는 여전히 금지.** 구매 동선은 기존 E 프롬프트 체인(트럭 카운터 실물 소품) — 새 키·새 무선 동사 0. 선글라스의 효과는 **정보뿐**(슬롯별 가격표 표시): 위협 가시성·속도 등 다른 특전을 붙이는 순간 v0.16 위협 가시성 계약과 충돌한다.
  - **점수는 v0.21부터 개수(등급표)가 아니라 슬롯별 가격이다** (개발자 "물건의 가격을 매기고, 같은 달걀이라도 달라야 돼"): `PRICE[slot]`은 `mulberry32(MAP_SEED^0x50524943)` **전용 서브스트림**에서 로드 시 1회 추첨 — 생성 스트림과 절대 공유 금지(공유하면 기존 시드 전부의 레이아웃이 조용히 바뀐다). 무선 비용 0(순수 시드 함수), R 재도전에도 가격 불변(가격은 맵 정체성). **범위는 일부러 겹친다**(계란 5..15 / 고기 12..28 / 큰알 20..40) — 겹침이 없으면 kind가 전순서라 가격을 읽을 이유가 없다. 정전 ×2는 여전히 유일한 배수, 요리 보너스는 가산(×10 환산: +10/+10/+20), 숯 5점. 자가검사는 **범위 정적 검사만** — 특정 시드의 추첨 결과에 단정을 걸면 그 시드의 멀티 합류가 통째로 죽는다. 가격표는 착용자 전용·rest 상태 전용·가까운 6개·**벽에 가린다**(depthTest — 전리품 정보에 벽투시 특권 없음). 미니맵 금지 목록에 **가격**도 포함된다.
  - **몰래 리젠은 생성이 아니라 활성화다** (v0.21, 개발자 "처음부터 배치가 아니라 나중에 몰래 리젠" + 강도 질의에 "그 수준이면 된다" 확정): 닭 총원 10 불변 — 전 개체를 로드 시 생성하고, **자유 순찰자 4마리만 휴면**(state 'dormant')으로 시작해 밤의 25/40/55/70%에 **자기 자리에서** 깨어난다(트레이 지킴 6은 t=0 전원 가동 — "지키는 알은 항상 지켜진다"가 계약). 기상은 THREAT_ACT_MIN_R(140)+**시선 보류**(벽 차단 없는 300u 시선이면 못 깬다)+0.8s 퍼덕임 예고 — 전역 방송 없음. **스케줄은 어떤 이유로도 가속·지연되지 않는다**(소음 반응 스폰 가속은 패널 만장일치 기각 — 정밀 책정된 사살 대가의 이중 청구). 사살과 완전 직교: 죽은 슬롯은 계속 죽어 있다(영구 제거 약속 무손상). 너구리도 같은 술어로 40/70%에 활성화. 런타임 개체 push 금지(클라 인덱스 미러 붕괴) — 새 위협을 또 만들면 반드시 이 패턴(로드 생성+휴면+활성화)을 따를 것.
  - **"주문(쿼터) 시스템" 항목은 v0.13에서 사실상 반대 방향으로 해소됨** (2026-08-14): 쿼터 3개를 폐지하고 점수제로 전환. 설계 문서 핵심 결정 2가 총량제를 기각했던 근거(요리가 장식이 됨)는 요리 없는 프로토엔 적용되지 않는다는 판단. 상세: `docs/goal/chicken-heist-score-run.md`.
  - **"요리·열 상태변화" 금지 해제·소진** (2026-08-15, 개발자 결정 — "차별점" 세션, v0.15에서 구현): 설계 문서가 신규 시스템 예산을 애초에 이 항목(핵심 결정 4)에 배정해 뒀으므로 해제가 아니라 복귀다. 단 **주방 위치는 설계 문서와 다르다**: 문서는 트럭=주방·요리=다운타임(핵심 결정 6)인데, 개발자 지시("맵도 랜덤이니까 주방 위치도 랜덤으로")로 **1F 축사 내부 랜덤 셀의 병아리 보온등**이 됐다 — 요리가 다운타임이 아니라 원정 리스크가 되는 긴장은 "닭 옆에서 기다리는 게 프리미엄의 가격"으로 의도된 것. 오버쿡드식 스테이션·미니게임 금지는 유지(동사 = 놓고-기다리고-집기). 열은 kind가 아니라 필드(e.heat). 탄 엣지 decoyPulse 1회가 거위 honk와 같은 조항의 **두 번째이자 마지막** 닭 커플링이다 — 세 번째를 만들지 말 것. 상세: `docs/goal/chicken-heist-procgen-kitchen.md`.
  - **맵은 v0.15부터 시드 절차 생성** (실내 위상·트레이·닭·주방 배치만 — 마당·격자 치수·계단은 영구 고정): `?seed=` URL 파라미터가 맵의 정체성이다. 멀티 초대 링크는 seed를 자동으로 싣고, 참가 게이트는 hi={버전,시드,해시} 3층 — **시드 불일치만 자동 리로드로 자가치유하고, 해시 불일치(같은 시드·다른 레이아웃 = 결정론적 발산)는 절대 리로드하지 않는다**. 생성 술어는 `layoutChecks()` 단일 소스 — 생성 루프와 validate()가 같은 함수를 불러야 하며 복사본을 만들지 말 것. R = 같은 맵 재도전, 새 맵 = 새 시드 리로드(N)·새 방. 미니맵은 테스트 단계 임시 기본 ON(Tab 토글) — **닭·거위·알·파동은 어떤 버전에서도 그리지 않는 계약** (지도가 위협 채널이 되면 월드 읽기 투자가 죽는다).
  - **멀티플레이 구현 해제·완료** (2026-08-12, 개발자 승인 "멀티 계획 문서 승인 ㄱㄱ M1부터 끝까지"): v0.10에서 M1~M4 구현됨 — 스펙은 `docs/goal/chicken-heist-multiplayer.md` (패널 33결정), 운영은 `docs/multiplayer-deploy-20260812.md`. 계획 문서의 "무후회 금지선 5"는 여전히 유효 (targetOf 액세서 / zoneOf·groundY 순수 / 단수 전역 금지 / update(dt) 경유 / 조우 결정론). 서버는 `server.js` — **중계 전용**, 게임 판정을 서버로 옮기지 말 것. 릴레이는 2026-08-12 Railway에 배포됨 (`wss://ccl8.up.railway.app`, 프로젝트 `ccl8-relay`) — 친구 멀티 테스트 가능. 재배포는 저장소 루트에서 `railway up --ci`.
    - **"호스트 락 상실 = 전원 정지"(D13) 개정** (v0.20.10, 2026-08-18 개발자 "포커스 벗어났다고 일시정지는 누구 머리에서 나온거냐"): 포커스 이탈·포인터락 상실은 이제 전원 정지가 아니다 — ESC는 메뉴(계속하기/일시정지/처음부터/나가기)를 열 뿐 게임은 계속 돌고, 전원 정지는 메뉴의 **[일시정지] 명시 클릭뿐**이다. `document.hidden` 정지는 유지 — 정책이 아니라 물리(숨은 탭은 RAF가 멎어 시뮬이 실제로 못 돈다)의 정직한 방송이고, D13의 정신은 거기만 남는다. 솔로는 메뉴=일시정지 그대로(관례이고 피해자가 없다). 정지 방송은 기존 `pz`/`pw` 스냅샷 필드 재사용 — 새 넷 메시지를 만들지 말 것. 상세: `docs/goal/chicken-heist-esc-menu.md`.
- **밝은 구간 LOGIC 불변 원칙의 예외 1건** (2026-08-12, 개발자 "언제 잡으러 오는지 납득 가능하게"): v0.9 발각 게이지 — 시야 발각의 개시가 즉발에서 게이지 적분으로 바뀜 (반경 상수는 그대로). 정전 즉발과 소리 경로는 무변경. 상세: `docs/goal/chicken-heist-building-legible-ai.md`.
- **세 번째 예외 — 발각된 뒤의 행동** (v0.22, 2026-08-18 개발자 "몬스터가 너무 바보같아… 좀더 똑똑하게 잡기 어렵게"): 범위를 좁게 못 박는다 — 이번 예외는 **"발각된 뒤에 무엇을 하는가"만** 바꾼다. 인지 반경 0건·플레이어 속도 0건이고, 자가검사가 그 사실을 로드타임에 증명한다(범위 위반 검사 2줄). 예산 지출은 딱 넷: `CHK_CHASE 124→132`(= SPD_WALK 정확히 — "사냥 중인 닭은 내 걷는 속도"를 세계가 가르친다), `CHASE_TURN 150→280°/s`(회전 지름 54 < 자유 횡폭 57 = 처음으로 통로 안에서 돈다), `OUT_CHASE_MULT 0.75→0.80`, 그리고 **리쉬 → 예산/층 종결**. 나머지는 전부 버그 수리라 예산이 아니다.
  - **진단이 요청을 뒤집었다**: 닭은 느린 게 아니라 추격이 금지돼 있었다. 지속 운반 도주는 118.2로 이미 추격 124보다 느렸고(152.6은 6초짜리 버스트일 뿐), 플레이어를 살리던 건 ① **추격 시작 시점에 얼어붙은 앵커**로 재던 리쉬 280(셀 폭 300보다 좁다 = 방 밖으로 못 쫓아옴)과 ② 맨 숫자 170의 해제 조항이었다. 그래서 **속도를 올리는 대신 인위적 포기를 걷어냈다** — 그러면 빈손 지속(168.9 > 132)이 자동으로 밸브가 된다.
  - **CHK_LEASH 280은 반경 불변, 역할만 좁혔다**: 추격 경계 → `toInvest` 소집 게이트 전용. (자가검사 10이 `gap=350` 대비 상한 300을 강제하므로 리쉬를 키우는 길은 애초에 막혀 있다 — 키우려면 B1.x0을 옮겨야 하고 그건 전 시드의 레이아웃 해시를 터뜨린다.)
  - **추격 종결은 이제 거리가 아니라 셋뿐이다**: 예산 `CHASE_BUDGET=10`(어떤 이유로도 갱신 안 됨 = 밸브 2단) / 층 이탈(계단이 진짜 자산이 된다) / 짐 해제. 종결은 **전부 `breakChase()`를 지난다** — 조용히 뒤도는 전이 0개가 계약이고, `det[tgt]=0`이 핵심이다(0.05 넘으면 '?' 대신 게이지가 그려져 "포기했다"가 "다시 눈치채는 중"으로 읽힌다).
  - **유지(holdsLoot)와 인지(carriesEgg)를 분리했다** — 소비자는 추격 유지 조항 하나뿐. Q로 품에 넣으면 예전엔 전원이 170u에서 포기했다(짐은 100% 유지한 채). **carriesEgg를 넓혀서 고치지 말 것**: updDet 게이트까지 넓어져 품·수레 소지자가 사상 처음 시야 발각 대상이 되고, "밝은 구간은 손만"(v0.4 바이트 동일)이 깨진다. 소리 내어 읽으면 — 품은 아직 못 찾은 눈을 속이지, 이미 붙은 놈은 못 속인다.
  - **밀치기 탈출 버프 번복** (v0.20 "의도된 변경" 주석을 개발자 확정으로 뒤집음): 맞은 닭이 집에 가지 않고 다시 붙는다. 사거리 60 대 잡힘 28이라 마주 본 플레이어는 항상 먼저 때리고, 그 한 대가 추격을 지우면 펀치를 아는 사람에겐 AI 개선 전체가 무의미해진다. 짝으로 `PUSH_STUN 0.8→0.55`(A7: 스턴 < 쿨 0.70 — 아니면 M1 홀드가 영구 스턴락). 영구 제거는 여전히 글러브 KO 몫. **단 1대1 제자리 펀치 교착은 기하학적으로 남는다**(사거리 60 > 잡힘 28) — 실측에서 확인했고, 대가는 소음·밤 시계·반출 불가다.
  - **밸브 계약 A1~A12는 파생 기호로만 쓴다** (`CARRY_SUST`·`EMPTY_SUST` 등). 리터럴 152.6/118.2를 박으면 SPD_RUN·스태미나를 만질 때 연결이 조용히 끊긴다 — 실제로 너구리 자가검사가 152.6 리터럴로 드리프트 중이었고 A8로 교체했다.
  - **잡힘의 자비 제거**: 넉다운 시 200u 내 전원을 flap으로 재우던 루프에서 **추격 중인 놈만 뺐다**. 안전한 근거는 무적 시간이 아니라 구조다 — 넉다운은 손·품·수레를 다 털므로 항상 빈손이 되고, 그게 밸브 1단을 자동 실행한다.
  - 상세·측정 계획: `docs/goal/chicken-heist-relentless-chase.md`. 사후 실측 숙제(중앙 추격 길이·종결 사유 분포·잡힘률·점수 낙폭·B1 스윕 비율)는 같은 문서.
- **밝은 구간 LOGIC 불변의 두 번째 예외 — 축척 환산** (v0.16, 2026-08-16 개발자 "맵 전체를 작게, 길을 좁게"): 맵을 18% 면적으로 줄이면서 반경 상수 19개를 일괄 환산했다. **난이도 변경이 아니라 난이도 불변을 위한 단위 환산**이고, 근거는 4개 실측 지표다 — 순찰 시야 커버리지 14.70%(환산 후 14.54%) / 무리 경보 도달 1.75셀(1.75) / 리쉬 3.42셀(3.25) / 달리기 각성 0.108마리(0.107). 앞으로 기하를 만지면 **이 4개 지표를 재고 같은 값이 나오게 반경을 맞출 것.** 예외의 예외: `DET_NEAR`는 축척대로면 27인데 잡힘 반경 28과 겹쳐 게이지가 죽으므로 40(몸 축척과 맵 축척이 충돌하는 유일한 상수).
- **몸 축척은 동결** — PLR_R·CHK_R·cart.r·PICK_R·JUMP_V·SEG_H 등은 맵이 줄어도 안 준다. "세계는 줄고 사람은 안 준다"가 축소의 정의다. 속도도 불변(축소가 비등방이라 단일 배율이 없고, CHK_CHASE 124 < 운반 질주 152.6이 즉사 금지의 탈출 밸브).
- **램프 성역 해제** (v0.16, 개발자 "이게 안밝다고? 말이안돼"): 크림색의 원인은 앰비언트가 아니라 램프 `distance`(2600 = 축사 대각의 127%)와 `decay` 기본 1(선형)이었다. 이제 distance 300 + decay 2(역제곱), 세기는 오히려 상향. **밝기를 조절할 땐 앰비언트가 아니라 이 두 손잡이를 먼저 볼 것** — 앰비언트는 프레임 휘도의 3.6%뿐이라 두 라운드 연속 헛돌았다. 대가: 램프 없는 셀 27% → 정전이 무사건이 되지 않도록 천장 전구를 두 층 전 셀에 1개씩 깔았다(정전 = 클라이맥스 계약의 기계적 부품).
- **위협 가시성 계약** (v0.16, 개발자 "내눈에 보이지않는 몬스터여야돼"): 닭 몸통·벼슬·부리의 emissive를 제거해 **불빛 밖의 닭은 안 보인다**. 단 눈·시야 원·!·?·발각 게이지(전부 MeshBasic)는 **건드리지 않는다** — 몸통은 "어디 있나", 마커는 "나를 눈치챘나"를 나르고, 후자를 지우면 v0.9 게이지가 막아 둔 억울한 갑툭튀가 되살아난다. 이 분리를 깨지 말 것.
- **"어둡게" 기각 번복** (2026-08-15, 개발자 "원안대로 확 어둡게" — v0.6에서 정전 대비 붕괴로 기각됐던 안): v0.15에서 전면 감광 시행. 단 **렌더 전용**(HEMI 0.58→0.20, MOON 0.22→0.12, 하늘 0x060a14 — 어그로·시야 LOGIC 상수 0건 변경)이고 **램프는 무변경** — 0.20 베이스에선 램프 웅덩이가 체감 광량의 ~80%라 정전의 램프 일괄 소등이 오히려 더 극적이다(이게 기각 사유에 대한 답). 어둠 관련 밝기 상수를 만질 땐 플리커 공식(HEMI_DARK+j*(HEMI_LIT-HEMI_DARK))과 반드시 같은 커밋으로 — 리터럴로 되돌리면 정전 예고가 "번쩍"이 되는 역연출. 수치는 개발자 모니터 검수 대상 (`?bright=1` A/B).
- **음파 시각화는 범위 한정 해제** (2026-08-10). 착지점은 **정전 귀환 구간(본편 판 후반부)뿐**이다.
  - 불 켜진 본편 구간에는 여전히 금지: 파동 렌더는 정전 중에만, 어그로·시야 로직 상수는 밝은 구간에서 v0.4와 동일해야 한다 (2열 표의 LOGIC 열 불변 원칙 — goal 문서 참조).
  - ~~착수 조건: 본편 친구 반응 수집 이후~~ → **개발자 권한으로 해제 (2026-08-11, "불이 언제 꺼져?").** 친구 반응 0 상태에서 검증 변수가 늘어나는 리스크는 개발자가 수용했고, 완화책은 `?dark=0` 격리 플래그(개발자 로컬 전용 — 친구 링크는 항상 기본값).
  - 상세: 설계 문서 "정전 귀환 스테이지" 절 + `docs/goal/chicken-heist-maze-blackout.md`.
- **위협 배치는 술어다, 감각이 아니다** (v0.15.3에서 확정 — 같은 계급의 결함을 세 번 연속 개별 수리한 뒤): 새 위협을 놓거나 기존 위협을 옮길 때는 반드시 `yardChokes()`(마당) / `gateChecks()`(실내 관문) 술어를 통과해야 한다.
  - 마당: 위협은 **모든 요충지**에서 `무발동 반경 + CHOKE_M(80)` 이상 떨어진다. 요충지는 데이터 테이블이므로 새 랜드마크를 추가하면 **거기 한 줄을 더한다** — 검사를 새로 쓰지 말 것.
  - 거리는 랜드마크 **중심이 아니라 플레이어가 실제로 설 수 있는 영역**까지 잰다 (`CHOKE_IN=PLR_R+2`). 중심 기준 측정이 문설주 2u 칼날을 통과시킨 장본인이다.
  - 상한도 검사한다: 요충지에서 띄우다가 필수 경로에 "뛰어도 안 깨는" 구간이 생기면 그것도 위반이다 (v0.14를 조용히 무의미하게 만드는 퇴행).
  - 거위 두 초소는 **x가 서로 달라야 한다** — 같으면 청음 원이 겹쳐 차선 서쪽에 공짜 질주 구간이 반드시 생긴다 (실측 159u).
  - 실내: 위협은 관문(정문·1F 계단·B1 계단)에 **상주도 통과도** 하지 않는다. "상주 금지"만으론 부족하다 — 실측상 상주 위반은 0%, 최단경로 통과 위반이 15.65%였다.
  - **활성화 최소 이격** (v0.19, 개발자 확정 "모든 몬스터에 해당하는 규칙"): 위협의 어떤 활성화 이벤트(KO 기상·스폰·리스폰 일체)도 활성 플레이어 `THREAT_ACT_MIN_R`(140) 안에서 일어나지 않는다. 지금 소비자는 KO 기상 하나지만(리스폰 시스템은 없다), 앞으로 위협을 등장시키는 코드는 전부 이 상수를 지나야 한다. 기상은 눌림 해제 후에도 0.8s 예고(퍼덕임+꽥)를 거친다 — 갑툭튀 금지의 활성화판.
- 설계 문서의 "미해결 질문"을 임의로 확정하지 말 것. 구현상 결정이 불가피하면 가장 싼 임시안을 쓰고 코드에 `// 임시:` 주석으로 표시.

## 설정 데이터 (v0.23~)
- **설정값의 진실은 `monsters.json`이다** (2026-08-19 개발자 "몬스터 정보를 따로 json으로 빼서 그거 로드해서 그거가지고 만들게" + 형태 질의에 "별도 .json 파일까지"). 같은 대화에서 먼저 내린 "내장 기본값 + 붙여넣기" 결정을 **개발자 권한으로 번복**한 것이고, 그때 명시된 두 대가(file:// 더블클릭 실행이 죽음 / 로컬 사본 친구와 조용히 어긋남)는 수용이 아니라 **완화**했다 — ①은 동기 XHR 실패 시 리터럴 폴백 + 타이틀 배지, ②는 합류 게이트 4번째 층(튜닝 해시)으로 조용한 어긋남을 명시적 거부로.
- **상수 선언과 주석은 절대 옮기지 않는다.** 숫자만 `T('이름',기본값)`으로 감싼다. 그 구역의 주석 30여 줄이 "왜 132이고 124가 아닌가"를 적어 둔 이 프로젝트의 결정 기록이라, 데이터 블록으로 옮기면 기록이 도구 관할로 넘어간다. 리터럴이 곧 폴백이므로 json이 없어도 게임은 돈다.
- **감싸면 안 되는 것 셋**: ① 파생값(`PUSH_CD=2*PUSH_STAGGER`·`KILL_CD=KILL_RAGE_T`·`HURT_STUN`·`CHASE_TURN` 라디안) — 양쪽을 따로 설정하게 두면 링크가 조용히 끊긴다(자가검사 25가 링크 생존을 단정한다). ② 구조값(`PER_TRAY`·`RACC_N`·`GOOSE_N`·`MAX_PLAYERS`·몸 반경) — 풀 크기·엔티티 수를 바꿔 **자가검사가 보고할 기회도 없이 로드 중 죽는다**. 채택 기준은 기계적이다: *노출 범위의 모든 값이 validate() 끝까지 도달하는 빌드를 만들어야 노출 가능*. ③ v0.22 동결 11개 — 자가검사가 리터럴 동일성으로 예외 범위를 증명하고 있다.
- **튜너(`dev/tuner.html`)는 검사를 복사하지 않는다.** 후보 설정을 sessionStorage에 쓰고 게임을 같은 출처 iframe으로 **콜드 부팅**해 `window.CCL8_VERDICT`를 읽는다. 콜드 부팅이라야 튜플 폭 잠금·살아있는 엔티티 검사·t=0 휴면 블록·시드 종속 요충지 루프가 전부 진짜 로드 순서로 돈다. `CCL8_CHECK(cfg)` 식 함수는 기각됐다 — 110개 단정을 후보 객체 참조로 고쳐 써야 하고, 그 순간 술어가 두 곳에 산다. 빨간 판정에선 저장·복사 버튼이 잠긴다.
- 새 손잡이를 늘리려면 `T()` 한 번 감싸면 끝이다 — 해시·검증 페이로드·튜너 목록이 전부 `TUNE_USED`에서 파생된다. 튜너에 메타를 안 적어도 '기타' 그룹에 자동으로 뜬다(도구가 키를 조용히 숨기지 못하게).
- 상세: `docs/goal/chicken-heist-tuner.md`.

## 기술 컨벤션
- 단일 HTML 파일, 바닐라 JS + Three.js r128 (cdnjs CDN). 빌드 도구 없음. (v0.23부터 `monsters.json` 1개 + `dev/tuner.html` 개발 도구 1개가 옆에 있지만, 게임 자체는 여전히 파일 하나로 돈다 — json이 없으면 기본값으로.)
- 로직은 평면(x,z)에서 돌고 렌더만 3D — `reference/sound-is-light-3d.html`의 구조를 따른다 (맵은 선분 seg, 충돌은 원-선분 밀어내기).
- 주석·UI 텍스트는 한국어.
- 수정 후 항상: `<script>` 블록 추출 → `node --check`로 문법 검증.
- 즉사 금지: 어떤 실패도 사망이 아니라 코미디(떨굼·넘어짐·쫓겨남)로 처리.

## 완성 판정
기능 목록 소화가 아니라 설계 문서의 성공 기준이다:
링크 받은 친구가 설명 없이 3초 안에 "훔쳐 나오는 거네"를 말하는가.
이 판정은 개발자가 직접 하므로, 빌드가 나오면 멈추고 보고할 것.
