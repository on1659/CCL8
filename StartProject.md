# StartProject — 양계장 수직절단 웹 프로토

이 파일이 진입점이다. 아래 순서대로 진행할 것.

## 0. 읽기

1. `CLAUDE.md` — 범위 규칙과 컨벤션 (자동으로 읽었을 것)
2. `docs/design-cooking-extraction-20260809.md` — 특히 "핵심 결정", "스코프 가드레일", "폐기·경유 목록", "기술 로드맵 > 1차"
3. `reference/sound-is-light-3d.html` — 재사용할 아키텍처:
   - 평면(x,z) 로직 + 3D 렌더 분리, 선분(seg) 기반 맵과 원-선분 충돌
   - 포인터록 1인칭 컨트롤, 단일 지오메트리 풀로 묶는 성능 패턴
   - 파동·음파 관련 코드는 이번 범위가 아님 — **구조만** 가져온다
   - `reference/sound-is-light-proto.html`은 같은 게임의 2D 버전 (참고용, 이식 대상 아님)

## 1. 만들 것 — `chicken-heist.html` (단일 파일, 저장소 루트)

- 맵 1개: 트럭(시작·반납 지점) + 마당 + 닭장 구역. 논리 평면은 reference와 같은 계열(960×600)에서 시작
- 계란: 잡기 / 들기(들면 이동 감속) / 던지기. 낙하·충돌 시 깨짐 물리 — 깨지면 그 자리에 흔적이 남고 회수 불가
- 닭 1종: 평상시 배회 → 계란을 집으면 어그로 → 추격 → 일정 거리 이탈 시 복귀. 플레이어가 잡혀도 즉사 금지 — 들고 있던 계란을 떨구고 잠깐 넘어지는 걸로 처리
- 반납: 트럭에 계란 3개 올리면 클리어 화면 (걸린 시간·깨뜨린 개수 표시)
- 카메라: 1인칭 ↔ 3인칭 **V키 토글**. 시점은 설계상 미해결 질문이므로 둘 다 만들어 A/B 테스트한다
- 조작 안내 HUD, R 재시작, M 음소거 (효과음은 에셋 없이 WebAudio 합성)

## 2. 작업 순서 (커밋 단위 제안)

1. 뼈대: 맵 + 이동 + 카메라 토글 (reference 이식)
2. 계란: 잡기·들기·던지기·깨짐
3. 닭: 배회·어그로·추격·복귀 + 잡힘 처리
4. 반납·클리어·HUD·사운드
5. 튜닝 상수를 파일 상단에 모으기 (이동 속도, 어그로 거리, 깨짐 임계, 닭 속도 등)

## 3. 금지

`CLAUDE.md`의 범위 규칙 참조. 요리·상점·주문·멀티·음파·추가 동물은 이번 범위가 아니다.

## 4. 완료 기준

- `node --check` 통과, 데스크톱 크롬에서 처음부터 끝까지 플레이 가능
- 최종 판정은 설계 문서의 성공 기준 — 링크 받은 친구가 설명 없이 3초 안에 "훔쳐 나오는 거네"를 말하는가. 이 판정은 개발자가 직접 하므로, 빌드가 나오면 멈추고 보고할 것
